enum metrics {
    sm = 8,
    md = 16,
    lg = 24,
    xg = 30,    
    borderRadius = 20,
    navBarHeight = 50
}

enum themeLight {
    background = '#f3f6fd',
    contentBackground = '#ffffff',
    iconColor = '#ccc',
    selectedIconColor = '#fff',
    selectedIconBackgroundColor = '#888888'
}

enum themeDark {
    background = '#000000',
    contentBackground = '#ffffff',
    iconColor = '#ccc',
    selectedIconColor = '#fff',
    selectedIconBackgroundColor = '#888888'
}


export { metrics, themeLight, themeDark }